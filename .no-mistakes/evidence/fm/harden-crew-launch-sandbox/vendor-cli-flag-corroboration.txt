Vendor CLI corroboration for the flags firstmate now launches with
Captured 2026-08-31T22:32:50Z on Darwin 27.0.0 (arm64)

Installed versions
  codex        codex-cli 0.150.1
  cursor-agent 2026.08.25-3e8eec8
  claude       2.1.252 (Claude Code)

== codex: the exact flag set firstmate now passes is accepted ==
$ codex -s workspace-write -a never -c sandbox_workspace_write.network_access=true --help
  exit=0  (flags parsed)
$ codex -s workspace-write -a never --not-a-real-flag --help      # negative control
  exit=2
  error: unexpected argument '--not-a-real-flag' found
  

codex --help, the three options that matter here:
    -s, --sandbox <SANDBOX_MODE>
        --dangerously-bypass-approvals-and-sandbox
    -a, --ask-for-approval <APPROVAL_POLICY>
  (the full-bypass flag still exists in the CLI; firstmate deliberately stopped using it)

== cursor-agent --help: what --auto-review, --sandbox and --force actually are ==
  -f, --force                  Force allow commands unless explicitly denied
  --yolo                       Alias for --force (Run Everything) (default:
  --auto-review                Use Auto-review (Smart Auto): a server classifier
  --sandbox <mode>             Explicitly enable or disable sandbox mode
  --trust                      Trust the current workspace without prompting

== claude: 'auto' is a real permission mode, distinct from bypassPermissions ==
$ CLAUDE_CODE_DISABLE_FAST_MODE=1 claude --permission-mode auto --help
  exit=0  (accepted)
$ claude --permission-mode not-a-mode --help                      # negative control
  exit=1
  error: option '--permission-mode <mode>' argument 'not-a-mode' is invalid. Allowed choices are acceptEdits, auto, bypassPermissions, manual, dontAsk, plan.

claude --help:
  --permission-mode <mode>              Permission mode to use for the session
  (choices: "acceptEdits", "auto",
